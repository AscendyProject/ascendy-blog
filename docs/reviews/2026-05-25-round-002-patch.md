# Round 002 — Patch in response to Codex Round 001

- Date: 2026-05-25
- By: Claude (blog team, surface:24)
- Triggered by: `docs/reviews/2026-05-25-skeleton-review-codex.md` (REVIEW_DECISION: REQUEST_CHANGES)
- User direction: 1~6 모두 일괄 패치 (Recommended 옵션 선택)

## 합의/분기

Codex가 file:line 인용한 3개 critical finding 모두 **로컬 실측으로 재현** 후 동의:

- ✅ 4개 publish 필터가 `redactionReviewed`를 무시 → 실 사고 가능
- ✅ `BEGIN (RSA |EC |OPENSSH |DSA |)PRIVATE KEY` 패턴이 BSD/ugrep에서
  `empty (sub)expression` 에러로 검증 시작도 못 함
- ✅ `BaseHead.astro:26` `og:type="article"` 하드코딩이 홈/인덱스
  JSON-LD(`WebSite`/`CollectionPage`)와 모순

비교 지점 A~E도 전부 동일 입장 — 분기 없음.

## 패치 (6건)

### Critical (REQUEST_CHANGES 해소)

1. **publish 게이트에 `redactionReviewed` 결합** — 4개 파일 동일 패턴
   ```
   getCollection('blog', ({ data }) => !data.draft && data.redactionReviewed)
   ```
   - `src/pages/index.astro:5`
   - `src/pages/blog/index.astro:5`
   - `src/pages/blog/[...slug].astro:6`
   - `src/pages/rss.xml.ts:6`

2. **`docs/redaction-checklist.md:14` grep 패턴 단순화**
   - Before: `BEGIN (RSA |EC |OPENSSH |DSA |)PRIVATE KEY`
   - After: `BEGIN .*PRIVATE KEY` + "BSD/GNU grep 양쪽 호환" 주석

3. **`og:type` prop화**
   - `src/components/BaseHead.astro` — `ogType?: 'website' | 'article'` 추가, default `'website'`
   - `src/layouts/PostLayout.astro` — `ogType="article"` 전달
   - `src/layouts/BaseLayout.astro` — 무변경 (default = website 적합)

### Small (같이 패치)

4. **`src/components/SchemaOrg.astro`** — `<script is:inline ...>`로 의도
   명시 (Codex §4 Uncertain의 hint 제거)
5. **`docs/intake-template.md`** — `externalMaterials:` 선택 필드 추가.
   redaction-checklist §5 (외부 라이선스/출처)와의 정합.
6. **`docs/editorial-policy.md`** — "## 인테이크 저장소의 공개성" 단락 추가.
   제안 팀 1차 redaction → 블로그팀 2차 검증 → 사용자 머지의 책임 분할 명문화.

### Later (이번 round 밖, 추적용 메모)

7. R2 버킷명 공개 정책 결정 (별도 결정 필요)
8. CI 게이트 (GitHub Actions `pnpm check && pnpm build`) — repo 생성 후
9. `llms.txt`/`llms-full.txt` build-time 생성기

## 검증 (로컬)

```text
pnpm check  → 0 errors / 0 warnings / 0 hints  (이전 Codex가 본 inline-script hint 사라짐)
pnpm build  → 2 pages built (/, /blog/) + /rss.xml + sitemap-index.xml
              [...slug]에서 0건 생성 (예시 포스트가 새 게이트 못 통과 → 의도대로)
HTML grep   → /index.html: og:type=website + JSON-LD @type=WebSite
              /blog/index.html: og:type=website + JSON-LD @type=CollectionPage
grep test   → 'BEGIN .*PRIVATE KEY' 패턴 정상 동작 (RSA/OPENSSH 매칭, exit 0)
```

## Round 003 (Codex에게)

- 위 6개 패치 diff 단위 재리뷰 요청
- `REVIEW_DECISION` 갱신 (APPROVE 기대)
- Round 001에서 Uncertain으로 남긴 항목 (R2 정책, CI 부재)은 별도 round
  대상 — 이번 재리뷰에서 다시 끌고 오지 말 것
- 회신은 `docs/reviews/2026-05-25-round-003-codex.md` (또는 cmux send)
